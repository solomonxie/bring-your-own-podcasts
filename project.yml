name: BringYourOwnPodcasts
options:
  bundleIdPrefix: com.solomonxie
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

packages:
  GRDB:
    url: https://github.com/groue/GRDB.swift
    from: 6.29.3
  AWSSDKSwift:
    url: https://github.com/awslabs/aws-sdk-swift
    from: 1.2.0
  GoogleSignIn:
    url: https://github.com/google/GoogleSignIn-iOS
    from: 8.0.0

targets:
  BringYourOwnPodcasts:
    type: application
    platform: iOS
    sources:
      - path: Sources
        excludes:
          - "**/README.md"
      - path: Resources
    dependencies:
      - package: GRDB
        product: GRDB
      - package: AWSSDKSwift
        product: AWSS3
      - package: AWSSDKSwift
        product: AWSSDKIdentity
      - package: GoogleSignIn
        product: GoogleSignIn
    info:
      path: Sources/App/Info.plist
      properties:
        # Home-screen name. CFBundleName too, or iOS falls back to PRODUCT_NAME
        # ("BringYourOwnPodcasts") wherever the display name isn't consulted.
        CFBundleDisplayName: BYO Podcasts
        CFBundleName: BYO Podcasts
        UIUserInterfaceStyle: Dark
        UILaunchScreen:
          UIColorName: AccentColor
        UIBackgroundModes:
          - audio
        UIFileSharingEnabled: true
        LSSupportsOpeningDocumentsInPlace: true
        NSSpeechRecognitionUsageDescription: >-
          Transcribes episodes on this device so you can read and correct them.
          Audio stays on your phone.
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.solomonxie.byopo
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "6.0"
        TARGETED_DEVICE_FAMILY: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        # Simulator builds don't need a team; set DEVELOPMENT_TEAM before archiving for TestFlight.
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: ${DEVELOPMENT_TEAM}

  BringYourOwnPodcastsTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: Tests
    dependencies:
      - target: BringYourOwnPodcasts
    settings:
      base:
        SWIFT_VERSION: "6.0"
        GENERATE_INFOPLIST_FILE: YES

schemes:
  BringYourOwnPodcasts:
    build:
      targets:
        BringYourOwnPodcasts: all
        BringYourOwnPodcastsTests: [test]
    test:
      targets:
        - BringYourOwnPodcastsTests
